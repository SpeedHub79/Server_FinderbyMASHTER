local HttpService = game:GetService("HttpService")
local TweenService = game:GetService("TweenService")
local TeleportService = game:GetService("TeleportService")
local Players = game:GetService("Players")
local MarketplaceService = game:GetService("MarketplaceService")
local UserInputService = game:GetService("UserInputService")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

local requestFunc

if syn and syn.request then
	requestFunc = syn.request
elseif http and http.request then
	requestFunc = http.request
elseif http_request then
	requestFunc = http_request
elseif fluxus and fluxus.request then
	requestFunc = fluxus.request
elseif request then
	requestFunc = request
else
	requestFunc = function()
		return nil
	end
end

local gameName = "Unknown"

pcall(function()
	local info = MarketplaceService:GetProductInfo(game.PlaceId)
	gameName = info.Name or "Unknown"
end)

local oldGui = PlayerGui:FindFirstChild("LowServerFinder")
if oldGui then
	oldGui:Destroy()
end

local function MakeDraggable(object)
	local dragging = false
	local dragStart
	local startPos
	local dragInput

	object.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = object.Position

			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then
					dragging = false
				end
			end)
		end
	end)

	object.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			dragInput = input
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if input == dragInput and dragging then
			local delta = input.Position - dragStart

			object.Position = UDim2.new(
				startPos.X.Scale,
				startPos.X.Offset + delta.X,
				startPos.Y.Scale,
				startPos.Y.Offset + delta.Y
			)
		end
	end)
end

local Gui = Instance.new("ScreenGui")
Gui.Name = "LowServerFinder"
Gui.ResetOnSpawn = false
Gui.IgnoreGuiInset = true
Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
Gui.Parent = PlayerGui

local LogoHolder = Instance.new("Frame")
LogoHolder.Name = "FloatingLogo"
LogoHolder.Parent = Gui
LogoHolder.AnchorPoint = Vector2.new(0.5, 0)
LogoHolder.Position = UDim2.new(0.5, 0, 0, 18)
LogoHolder.Size = UDim2.new(0, 70, 0, 70)
LogoHolder.BackgroundTransparency = 1
LogoHolder.ZIndex = 100

local LogoGlow = Instance.new("Frame")
LogoGlow.Parent = LogoHolder
LogoGlow.AnchorPoint = Vector2.new(0.5, 0.5)
LogoGlow.Position = UDim2.new(0.5, 0, 0.5, 0)
LogoGlow.Size = UDim2.new(1, 12, 1, 12)
LogoGlow.BackgroundColor3 = Color3.fromRGB(125, 70, 255)
LogoGlow.BackgroundTransparency = 0.72
LogoGlow.BorderSizePixel = 0
LogoGlow.ZIndex = 99

local GlowCorner = Instance.new("UICorner")
GlowCorner.CornerRadius = UDim.new(1, 0)
GlowCorner.Parent = LogoGlow

local Logo = Instance.new("TextButton")
Logo.Parent = LogoHolder
Logo.AnchorPoint = Vector2.new(0.5, 0.5)
Logo.Position = UDim2.new(0.5, 0, 0.5, 0)
Logo.Size = UDim2.new(0, 60, 0, 60)
Logo.BackgroundColor3 = Color3.fromRGB(111, 60, 215)
Logo.BorderSizePixel = 0
Logo.AutoButtonColor = false
Logo.Text = ""
Logo.ZIndex = 101

local LogoCorner = Instance.new("UICorner")
LogoCorner.CornerRadius = UDim.new(1, 0)
LogoCorner.Parent = Logo

local LogoGradient = Instance.new("UIGradient")
LogoGradient.Color = ColorSequence.new({
	ColorSequenceKeypoint.new(0, Color3.fromRGB(210, 145, 255)),
	ColorSequenceKeypoint.new(0.35, Color3.fromRGB(145, 75, 245)),
	ColorSequenceKeypoint.new(0.7, Color3.fromRGB(92, 42, 190)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(55, 25, 125))
})
LogoGradient.Rotation = 135
LogoGradient.Parent = Logo

local LogoStroke = Instance.new("UIStroke")
LogoStroke.Color = Color3.fromRGB(235, 210, 255)
LogoStroke.Thickness = 2
LogoStroke.Transparency = 0.05
LogoStroke.Parent = Logo

local LogoIcon = Instance.new("TextLabel")
LogoIcon.Parent = Logo
LogoIcon.AnchorPoint = Vector2.new(0.5, 0.5)
LogoIcon.Position = UDim2.new(0.5, 0, 0.42, 0)
LogoIcon.Size = UDim2.new(0, 38, 0, 30)
LogoIcon.BackgroundTransparency = 1
LogoIcon.Font = Enum.Font.GothamBlack
LogoIcon.Text = "⌁"
LogoIcon.TextColor3 = Color3.fromRGB(255, 255, 255)
LogoIcon.TextSize = 31
LogoIcon.ZIndex = 102

local LogoText = Instance.new("TextLabel")
LogoText.Parent = Logo
LogoText.AnchorPoint = Vector2.new(0.5, 1)
LogoText.Position = UDim2.new(0.5, 0, 0.92, 0)
LogoText.Size = UDim2.new(1, 0, 0, 15)
LogoText.BackgroundTransparency = 1
LogoText.Font = Enum.Font.GothamBold
LogoText.Text = "SF"
LogoText.TextColor3 = Color3.fromRGB(255, 255, 255)
LogoText.TextSize = 9
LogoText.ZIndex = 102

local LogoScale = Instance.new("UIScale")
LogoScale.Parent = LogoHolder
LogoScale.Scale = 1

local Main = Instance.new("Frame")
Main.Name = "MainFrame"
Main.Parent = Gui
Main.AnchorPoint = Vector2.new(0.5, 0.5)
Main.Position = UDim2.new(0.5, 0, 0.53, 0)
Main.Size = UDim2.new(0, 690, 0, 445)
Main.BackgroundColor3 = Color3.fromRGB(12, 11, 18)
Main.BorderSizePixel = 0
Main.ZIndex = 10

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 22)
MainCorner.Parent = Main

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(91, 61, 145)
MainStroke.Thickness = 1.4
MainStroke.Transparency = 0.15
MainStroke.Parent = Main

local MainGradient = Instance.new("UIGradient")
MainGradient.Color = ColorSequence.new({
	ColorSequenceKeypoint.new(0, Color3.fromRGB(27, 21, 39)),
	ColorSequenceKeypoint.new(0.5, Color3.fromRGB(16, 14, 24)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(9, 9, 14))
})
MainGradient.Rotation = 90
MainGradient.Parent = Main

local Header = Instance.new("Frame")
Header.Parent = Main
Header.Size = UDim2.new(1, 0, 0, 88)
Header.BackgroundColor3 = Color3.fromRGB(25, 19, 37)
Header.BorderSizePixel = 0
Header.ZIndex = 11

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 22)
HeaderCorner.Parent = Header

local HeaderBottom = Instance.new("Frame")
HeaderBottom.Parent = Header
HeaderBottom.Position = UDim2.new(0, 0, 0.55, 0)
HeaderBottom.Size = UDim2.new(1, 0, 0.45, 0)
HeaderBottom.BackgroundColor3 = Color3.fromRGB(25, 19, 37)
HeaderBottom.BorderSizePixel = 0
HeaderBottom.ZIndex = 11

local HeaderAccent = Instance.new("Frame")
HeaderAccent.Parent = Header
HeaderAccent.Position = UDim2.new(0, 0, 1, -2)
HeaderAccent.Size = UDim2.new(1, 0, 0, 2)
HeaderAccent.BackgroundColor3 = Color3.fromRGB(139, 77, 245)
HeaderAccent.BorderSizePixel = 0
HeaderAccent.ZIndex = 13

local HeaderLogo = Instance.new("Frame")
HeaderLogo.Parent = Header
HeaderLogo.Position = UDim2.new(0, 18, 0.5, -25)
HeaderLogo.Size = UDim2.new(0, 50, 0, 50)
HeaderLogo.BackgroundColor3 = Color3.fromRGB(108, 57, 205)
HeaderLogo.BorderSizePixel = 0
HeaderLogo.ZIndex = 12

local HeaderLogoCorner = Instance.new("UICorner")
HeaderLogoCorner.CornerRadius = UDim.new(0, 15)
HeaderLogoCorner.Parent = HeaderLogo

local HeaderLogoGradient = Instance.new("UIGradient")
HeaderLogoGradient.Color = ColorSequence.new({
	ColorSequenceKeypoint.new(0, Color3.fromRGB(196, 122, 255)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(74, 34, 160))
})
HeaderLogoGradient.Rotation = 45
HeaderLogoGradient.Parent = HeaderLogo

local HeaderLogoIcon = Instance.new("TextLabel")
HeaderLogoIcon.Parent = HeaderLogo
HeaderLogoIcon.Size = UDim2.new(1, 0, 0.7, 0)
HeaderLogoIcon.Position = UDim2.new(0, 0, 0.05, 0)
HeaderLogoIcon.BackgroundTransparency = 1
HeaderLogoIcon.Font = Enum.Font.GothamBlack
HeaderLogoIcon.Text = "⌁"
HeaderLogoIcon.TextColor3 = Color3.fromRGB(255, 255, 255)
HeaderLogoIcon.TextSize = 24
HeaderLogoIcon.ZIndex = 13

local HeaderLogoText = Instance.new("TextLabel")
HeaderLogoText.Parent = HeaderLogo
HeaderLogoText.Size = UDim2.new(1, 0, 0, 13)
HeaderLogoText.Position = UDim2.new(0, 0, 0.7, 0)
HeaderLogoText.BackgroundTransparency = 1
HeaderLogoText.Font = Enum.Font.GothamBold
HeaderLogoText.Text = "SF"
HeaderLogoText.TextColor3 = Color3.fromRGB(255, 255, 255)
HeaderLogoText.TextSize = 8
HeaderLogoText.ZIndex = 13

local Title = Instance.new("TextLabel")
Title.Parent = Header
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 82, 0, 14)
Title.Size = UDim2.new(0, 400, 0, 30)
Title.Font = Enum.Font.GothamBold
Title.Text = "Server Finder"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 23
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.ZIndex = 12

local Subtitle = Instance.new("TextLabel")
Subtitle.Parent = Header
Subtitle.BackgroundTransparency = 1
Subtitle.Position = UDim2.new(0, 83, 0, 45)
Subtitle.Size = UDim2.new(0, 400, 0, 18)
Subtitle.Font = Enum.Font.Gotham
Subtitle.Text = "Find available public servers"
Subtitle.TextColor3 = Color3.fromRGB(157, 147, 174)
Subtitle.TextSize =
